- name: Close TCP ports for Jellyfin
  become: true
  ansible.posix.firewalld:
    port: "{{ item }}/tcp"
    permanent: yes
    state: disabled
    immediate: true
  loop: "{{ morla_jellyfin_fw_ports.tcp }}"

- name: Close UDP ports for Jellyfin
  become: true
  ansible.posix.firewalld:
    port: "{{ item }}/udp"
    permanent: yes
    state: disabled
    immediate: true
  loop: "{{ morla_jellyfin_fw_ports.udp }}"

