---
- name: Add Container to the list of public domains
  ansible.builtin.set_fact:
    morla_public_domains: "{{ morla_public_domains + [ morla_sonarr_public_url ] }}"
  when: ( morla_sonarr_flag_public is defined ) and ( morla_sonarr_flag_public|bool == True ) 
 
- name: Add Container to the list of Wireguard accessible containers
  ansible.builtin.set_fact:
    morla_wireguard_domains: "{{ morla_wireguard_domains + [ { 'name': morla_sonarr_short_name, 'port': morla_sonarr_reverse_proxy_port } ] }}"
  when: ( morla_sonarr_flag_wireguard is defined ) and ( morla_sonarr_flag_wireguard|bool == True ) 

- name: Add Container to the list of autostart containers
  ansible.builtin.set_fact:
    morla_systemd_autostart_containers: "{{ morla_systemd_autostart_containers + [ morla_sonarr_container_name ] }} "
  when: ( morla_sonarr_flag_autostart is defined ) and ( morla_sonarr_flag_autostart|bool == True ) 


